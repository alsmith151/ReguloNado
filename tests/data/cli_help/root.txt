                                                                                                    
 Usage: root [OPTIONS] COMMAND [ARGS]...                                                            
                                                                                                    
 Build datasets, train models, run inference, and manage workflows.                                 
                                                                                                    
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --version                           Show Python package and Rust extension versions.             │
│ --log-level                   TEXT  Log level for the 'regulonado' logger. [default: INFO]       │
│ --quiet                             Shorthand for --log-level WARNING.                           │
│ --verbose             -v            Shorthand for --log-level DEBUG. Takes priority over         │
│                                     --quiet.                                                     │
│ --install-completion                Install completion for the current shell.                    │
│ --show-completion                   Show completion for the current shell, to copy it or         │
│                                     customize the installation.                                  │
│ --help                              Show this message and exit.                                  │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Commands ───────────────────────────────────────────────────────────────────────────────────────╮
│ config         Generate a workflow config, interactively or from flags.                          │
│ init           Install Snakemake execution presets into ~/.config/snakemake/.                    │
│ train          Train one model from a named preset.                                              │
│ train-regions  Train a region-count head on cached backbone embeddings from a named preset.      │
│ dataset        Build an Arrow DatasetDict from BED / FASTA / a pre-assembled track table.        │
│ predict        Generate BigWig tracks from trained model predictions, collapsing equal bins.     │
│ design         Mutate endogenous enhancer candidates to sharpen cell-type specificity.           │
│ pipeline       Run the packaged Snakemake workflow, optionally selecting one stage.              │
│ normalization  Infer, correct, and apply per-track signal normalisation factors.                 │
│ tracks         Discover, QC, and assemble tracks.parquet before the dataset build.               │
│ attribute      Score attribution profiles and/or call cores from in-silico mutagenesis.          │
│ counts         Count BAM events over regions and gather them into a RegionCountData run.         │
│ embed          Cache frozen-backbone embeddings over region targets, for training a count head.  │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯

