                                                                                                    
 Usage: root train [OPTIONS] DATASET                                                                
                                                                                                    
 Train one model from a named preset.                                                               
                                                                                                    
 Use ``--set`` for less common settings, for example                                                
 ``--set trainer.max_eval_samples=200``. Use ``regulonado pipeline`` when                           
 several independent runs or warm-start phases should be orchestrated together.                     
                                                                                                    
╭─ Arguments ──────────────────────────────────────────────────────────────────────────────────────╮
│ *    dataset      PATH  Training dataset: the profile dataset directory (trunk live), or the     │
│                         region-count dataset from 'counts gather' (trunk cached)                 │
│                         [required]                                                               │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --trunk                                 TEXT     live: run (and optionally fine-tune) the trunk  │
│                                                  every step; cached: train a head on embeddings  │
│                                                  cached by 'embed regions'                       │
│                                                  [default: live]                                 │
│ --embeddings                    -e      PATH     Embedding cache directory (trunk cached)        │
│ --target-group                          TEXT     Set data.target_group (trunk cached), e.g.      │
│                                                  HL-60                                           │
│ --output-dir                    -o      PATH     Run directory for checkpoints and diagnostics   │
│ --preset                        -p      TEXT     Named phase preset: python/configs/experiment/  │
│                                                  (trunk live, default head_only) or              │
│                                                  python/configs/cached_experiment/ (trunk        │
│                                                  cached, default pretrain)                       │
│ --metadata                              PATH     tracks.parquet to use instead of the dataset    │
│                                                  copy                                            │
│ --nproc-per-node                        INTEGER  Use torchrun with this many local processes     │
│                                                  when >1                                         │
│                                                  [default: 1]                                    │
│ --resume-from-checkpoint                TEXT     Full Trainer resume from a checkpoint dir, or   │
│                                                  'true' for latest in output-dir                 │
│ --init-weights-from-checkpoint          PATH     Warm start from model weights only with a fresh │
│                                                  optimizer/scheduler                             │
│ --max-steps                             INTEGER  Override trainer.max_steps                      │
│ --batch-size                            INTEGER  Override per-device train batch size            │
│ --eval-batch-size                       INTEGER  Override per-device eval batch size             │
│ --learning-rate,--lr                    FLOAT    Override head learning rate                     │
│ --backbone-lr                           FLOAT    Override backbone learning rate                 │
│ --num-workers                           INTEGER  Override DataLoader worker count                │
│ --no-wandb                                       Disable W&B reporting for this run              │
│ --set                                   TEXT     Override one setting as KEY=VALUE (repeatable)  │
│ --print-config                                   Print the resolved training config and exit     │
│ --schedule-only                                  Read Parquet metadata, print the resolved       │
│                                                  schedule, and exit before model setup           │
│ --dry-run                                        Print the resolved command without running it   │
│ --help                                           Show this message and exit.                     │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯

