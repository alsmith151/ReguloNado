# Head family selected by build_perturb_head.
type: transfer_mlp
# Hidden channel width inside the transfer head.
hidden: 512
# Dropout applied inside the transfer MLP.
dropout: 0.0
# Intermediate width used by the metadata-free transfer MLP.
mlp_hidden: 1024