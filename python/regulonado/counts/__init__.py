"""Per-region read counts straight from BAMs, and the parquet dataset built from them.

See :mod:`regulonado.counts.bam` (event counting) and
:mod:`regulonado.counts.dataset` (the on-disk ``RegionCountData`` layout).
"""

from __future__ import annotations
